---
title: Frank Noiro
description: Writings from Brooklyn, a digital garden.
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{%- if title -%}{{title}} |&nbsp;{%- endif -%}f.noirot's garden</title>
    <meta name="title" content="{%- if title -%}{{title}} |&nbsp;{%- endif -%}f.noirot's garden" />
    <meta name="description" content="{{ description }}" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Alegreya:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">    <link rel="preload" href="/styles/fonts.css" as="style">
    <link rel="stylesheet" href="/styles/base.css">
    {% if stylesheet %}
    <link rel="stylesheet" href="/styles/{{ stylesheet }}.css">
    {% endif %}
  </head>
  <body>
    {% include 'components/nav.njk' %}
    <main>
    {{ content | safe }}
    </main>
    {% include 'components/footer.njk' %}
  </body>
</html>
