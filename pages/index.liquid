---
title: Frank Noirot's digital garden
layout: layouts/base.njk
---
<h1>{{ title }}</h1>

Here are my personal notes and writing. This is a digital garden, so all work is in-progress.

{% for type in contentTypes %}
<a href="/{{ type.name }}">
  <h2>{{type.name | capitalize }}&nbsp;<small>{{ collections[type.name].length }} items<small></h2></a>
<ul class="grid">
  {%- assign sortedCollection = collections[type.name] | sortByKey: 'data.updated', true | head: 15 -%}
  {% for item in sortedCollection %}
  {% include 'components/grid-card' type: type.name item: item %}{% endfor %}
</ul>{% endfor %}
